addons:
    apt:
        packages:
            - cmake
            - gfortran

script:
    - mkdir build tests
    - cd build
    - cmake ..
    - make

after_script:
    - cd ../scripts
    - python run_tests.py --cores 2 --exec ../build/HAZ --rtol 0.001

